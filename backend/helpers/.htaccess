# Deny direct HTTP access to all files in this directory.
# These PHP files are only included via require_once() from API endpoints.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order Allow,Deny
    Deny from all
</IfModule>
